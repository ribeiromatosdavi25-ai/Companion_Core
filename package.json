{
  "name": "companion",
  "private": true,
  "packageManager": "pnpm@8.15.4",
  "workspaces": [
    "shared",
    "services/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  }
}
